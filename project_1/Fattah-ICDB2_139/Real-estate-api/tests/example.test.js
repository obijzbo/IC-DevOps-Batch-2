import { describe, it, expect } from 'vitest';

describe('Example Test', () => {
  it('should pass', () => {
    expect(1 + 1).toBe(2);
  });

  it('should pass', () => {
    expect(1 + 3).toBe(4);
  });

  it('should pass', () => {
    expect(3 + 3).toBe(6);
  });
});

